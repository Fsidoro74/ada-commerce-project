# Normalize text files to use LF in the repository and native line endings on checkout
* text=auto

# Treat common code files as text explicitly
*.java text
*.md text
*.gradle text
*.xml text
*.properties text
*.yml text
*.yaml text

# Keep binary files untouched
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.jar binary
